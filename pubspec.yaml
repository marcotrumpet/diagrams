name: diagrams
description: An app that let you draw diagrams.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # system_tray: ^0.0.9
  # menubar:
  #   git:
  #     url: https://github.com/google/flutter-desktop-embedding.git
  #     path: plugins/menubar
  freezed_annotation: 1.1.0
  flutter_bloc: 8.0.1
  bloc_test: 9.0.2
  get_it: 7.2.0
  golden_toolkit: 0.13.0
  matrix4_transform: 2.0.1
  # window_size:
  #   git:
  #     url: https://github.com/google/flutter-desktop-embedding.git
  #     path: plugins/window_size 
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0
  build_runner: 2.1.7
  freezed: 1.1.0

flutter:
  uses-material-design: true
